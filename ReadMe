The instruction works only for minikube setup:

Execute the following script  

sudo ./install_script.sh  

Pass the 'tag' you want to use for the docker image and the 'release name' for your helm application

After the script execution you can access the website at ---> trazler.info:<port-number-given-by-minikube>

To delete the release:

helm delete <release-name>